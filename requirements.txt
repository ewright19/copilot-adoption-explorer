# Install from the repository root before running bootstrap.py, preflight.py, or run.py:
#   python -m pip install -r requirements.txt
requests>=2.31,<3
openpyxl>=3.1,<4

# Secured web app dependencies. The static export only needs the packages above.
flask>=3.0,<4
msal>=1.28,<2
waitress>=3.0,<4
